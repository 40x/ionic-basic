(function(angular) {
    'use strict';
    angular
        .module('app.dashboardChild')
        .controller('DCController', DCController);

    // @ngInject
    function DCController() {
        var dVm = this;
    }
}(angular));
